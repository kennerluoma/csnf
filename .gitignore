node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
.env
.env.*
!.env.example
# Public build-time values only (Sanity project id + dataset: they appear in every image URL).
# Committed so builds work where secrets are withheld (Dependabot PRs, forks). Never put a token here.
!.env.production
.wrangler/
design/renders/*.png
design/assets/*
!design/assets/.gitkeep
design/shots/
design/agent-report.json
design/agent-stderr.log

# agency scratch (triage / intake passes)
.agency/issues.json
.agency/triage.json
.agency/drafts.json
.agency/intake.md
